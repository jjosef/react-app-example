{
  "database": {
    "rules": "config/firebase/database.rules.json"
  },
  "storage": {
    "rules": "config/firebase/storage.rules"
  },
  "hosting": {
    "public": "dist",
    "rewrites": [
      {
        "source": "**",
        "destination": "/index.html"
      }
    ]
  }
}
